<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.nhou.mapper.store.StoreMapper">

	<insert id="insert">
		INSERT ALL 
			INTO product (productName, price, stock, product_content)
			VALUES (#{productName}, #{price}, #{stock}, #{product_content})
		
			INTO productCategory (categoryName)
			VALUES (#{categoryName})
		
			INTO productFile (productImage)
			VALUES (#{productImage})
			
			<!-- ??? +구글링 -->

	</insert>
	
	<select id="list" resultType="com.nhou.domain.store.StoreDto">
		SELECT
			productId,
			productName,
			price,
		FROM 
			Product,  
<!-- 		JOIN productFile,
			ON
		JOINproductReply
			ON -->
		ORDER BY id DESC
		
	</select>

</mapper>
