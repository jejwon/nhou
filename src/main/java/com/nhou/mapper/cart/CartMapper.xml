<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nhou.mapper.cart.CartMapper">
	<!-- 장바구니 추가 -->
	<insert id="insert">
	INSERT INTO cart (product_productId, member_userId, count, option) 
	VALUES (#{product_productId}, #{member_userId}, #{count}, #{option})
	</insert>
	
	<!-- 결제 이전 장바구니 개별 삭제 -->
	<delete id="delete">
	DELETE FROM cart 
	WHERE cartId = #{cartId}
	</delete>
	
	<!-- 장바구니 수량 수정 -->
	<update id="update">
	UPDATE cart
	SET count = #{count} where cartId = #{cartId}
	</update>
	
	<!-- 장바구니 얻어오기 -->
	<select id="getByCartId" resultType="com.nhou.domain.cart.CartDto">
	SELECT 
		c.cartId,
		c.product_productId,
		c.member_userId,
		c.count,
		p.productName,
		p.price
	FROM cart c
			LEFT JOIN product p ON c.product_productId = p.productId
	</select>
	
	<!-- 카트 중복 수량 확인 -->
	<!-- <select id="getByUserId" resultType="com.nhou.domain.cart.CartDto">
		SELECT * FROM cart 
		WHERE member_userId = #{member_userId} and cartId = #{cartId}		
	</select> -->
	
	<select id="list" resultType="com.nhou.domain.cart.CartDto">
	SELECT 
		c.cartId,
		c.product_productId,
		c.member_userId,
		c.count,
		c.option,
		p.productName,
		p.price
	FROM cart c
			LEFT JOIN product p ON c.product_productId = p.productId
	</select>
	
	<!-- 결제 정보 담기 -->
	<insert id="payInsert">
	INSERT INTO order (orderDate, address1, address2, postal, receiverName, receiverPhone, payment, status, member_userId) 
	VALUES (#{orderDate}, #{address1}, #{address2}, #{postal}, #{receiverName}, #{receiverPhone}, #{payment}, #{status}, #{member_userId})
	</insert>
	
	
</mapper>